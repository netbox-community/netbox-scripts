# Configuration

## Overview

NetBox Custom Scripts currently defines no plugin settings. Enabling the
plugin in `PLUGINS` is sufficient. No `PLUGINS_CONFIG` entry is required.

```python
PLUGINS = [
    'netbox_custom_scripts',
]
```

Settings introduced by future releases will be documented here.
